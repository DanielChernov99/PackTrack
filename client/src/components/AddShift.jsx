import React from 'react';

const AddShift = ({ user }) => {
    return (
        <div>
            <h2>Add New Shift</h2>
            <p>Add shift form will be implemented here.</p>
        </div>
    );
};

export default AddShift;