import React from 'react';

const ShiftHistory = ({ user }) => {
    return (
        <div>
            <h2>Shift History</h2>
            <p>Your shift history will be displayed here.</p>
        </div>
    );
};

export default ShiftHistory;